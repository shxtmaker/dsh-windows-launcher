# D20 · 验证会话与来源隔离

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D18。默认下一建议：D21（仅建议，不在同轮自动执行）。

## 单一目标

证明隐藏、导航、会话变化与重放不会使附件进入错误目标。

## 允许改动范围

- `Linux双目标/多会话fixture`
- `来源和epoch测试`
- `必要局部修复`

## 执行步骤

1. 同时运行两个目标和会话，覆盖同主机不同端口、已存在附件与新会话。
2. 测试切会话、刷新、删除、后台页、iframe、跨源导航和过期消息。
3. 验证无session首页、锁定composer、子代理和未知版本明确拒绝。

## 必须交付

- 隔离矩阵及目标/会话/操作映射证据。

## 本任务验收

- 所有错投计数为零；拒绝不泄露本机路径/字节。
- 桌面真实焦点及WebView来源保留WindowsPending。

## 失败与停止条件

身份不可靠不得增加自动重试或仅按host放行；保存最小竞态案例。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
