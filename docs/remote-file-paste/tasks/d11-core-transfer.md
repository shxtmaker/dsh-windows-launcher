# D11 · 实现可移植分块协调器

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D01、D10。默认下一建议：D12（仅建议，不在同轮自动执行）。

## 单一目标

用真实生产Core完成限额、分块、背压、取消及身份绑定。

## 允许改动范围

- `src/DshLauncher.Core/Attachments/`
- `Core.Tests/Attachments/`

## 执行步骤

1. 256KiB原始块、最多两块在途、每目标一次一个文件，全局并发可控。
2. 通过真实接口注入字节源/通道/时钟，处理短读、源错误、乱序、超时、取消和流释放。
3. 测试已确认成功项不重导、去重缓存有界及document/session失效。

## 必须交付

- 生产状态机、行为测试和协议trace。

## 本任务验收

- Linux直接运行该Core；所有待确认字节、队列、缓存都有上界。
- 没有Windows API，没有全文件Base64或影子实现。

## 失败与停止条件

测试不稳需可控时钟/通道复现，不用延长sleep掩盖竞态。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
