# D19 · 验证上传网络错误与撤销语义

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D18。默认下一建议：D20（仅建议，不在同轮自动执行）。

## 单一目标

覆盖真实远程上传的失败、恢复与不确定结果，禁止重复发送。

## 允许改动范围

- `Linux故障代理/网络fixture`
- `上传和重试测试`
- `必要局部修复`

## 执行步骤

1. 注入断流、超时、413、HTTP200业务失败、403/401和宿主重启。
2. 普通error走原draft重试；失效ready receipt提示移除重导，不重放成功项。
3. 区分撤销后新请求拒绝与在途请求终止；验证客户端取消。

## 必须交付

- 可重复故障测试及脱敏trace。

## 本任务验收

- 错误可见、数据不乱投、消息不自动重发；不同拒绝语义被正确记录。

## 失败与停止条件

只修本任务问题，新增无关需求登记不实现；不能通过弱化鉴权恢复成功。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
