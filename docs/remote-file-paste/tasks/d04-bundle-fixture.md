# D04 · 启动隔离的真实 Harness 与全家桶夹具

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D03。默认下一建议：D05（仅建议，不在同轮自动执行）。

## 单一目标

在 Linux 重复启动真实 Harness＋all＋骨架插件，不要求尚未实现的文件功能通过。

## 允许改动范围

- `plugins/dsh-remote-attachments/tests/fixtures/`
- `测试启动/清理脚本`
- `兼容版本锁定清单`

## 执行步骤

1. 创建私有 DSH_HOME、profile、工作区和固定版本安装；隔离生产凭据。
2. 安装真实 all 与 D03 tarball，记录实际 host/client 模块来源及数量。
3. 在测试环境限制更新、自动隧道与无关外网副作用；提供启动就绪检测、超时及仅清理自有资源的退出逻辑。

## 必须交付

- 可重复的一键 fixture 启停命令与版本/integrity清单。
- 服务与全家桶页面真实加载证据。

## 本任务验收

- remote 和 addon 的 host/client 各按预期挂载，未额外安装 remote。
- 原页面/设置可加载，重复启动与清理不污染其他 profile。

## 失败与停止条件

全家桶模块降级或缺失必须暴露；不能改用模拟网页继续算通过。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
