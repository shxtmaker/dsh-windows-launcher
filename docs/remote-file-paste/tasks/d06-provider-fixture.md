# D06 · 建立确定性模型与真实工具读取夹具

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D04。默认下一建议：D07（仅建议，不在同轮自动执行）。

## 单一目标

无需真实模型账号，验证本地 stub 能驱动真实 Harness 工具执行并校验返回。

## 允许改动范围

- `plugins/dsh-remote-attachments/tests/fixtures/provider/`
- `测试工具调用协议适配`

## 执行步骤

1. 以固定版本实际 provider/工具协议实现本地确定性 stub。
2. 先用普通文本任务让真实工具读取自有 fixture 文件，验证工具名、路径映射和调用结果。
3. 提供图像载荷记录与受控请求计数，为后续附件发送建立断言。

## 必须交付

- provider stub、工具读回基线、测试请求计数器。

## 本任务验收

- 工具结果由真实 Harness 工具产生，不是 stub 伪造。
- 测试不用付费账户；fixture 内容及哈希可重复。

## 失败与停止条件

工具路径或协议不通保持本任务未完成；不得以模型文字“已收到”冒充读回。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
