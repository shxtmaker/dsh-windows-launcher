# D17 · 完成 WebView2 与生产 Core 组合

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D13、D16。默认下一建议：D18（仅建议，不在同轮自动执行）。

## 单一目标

Windows端端到端代码完整，消息转发复用Core，不在事件处理器重写状态机。

## 允许改动范围

- `Desktop/Attachments/RemoteAttachmentBridge`
- `页面session组合根`
- `生产通道适配接口`

## 执行步骤

1. 将原生输入、暂存字节源、Core与WebView通道连接；每次收发再次检查身份和大小。
2. 主动取消、迟到响应、异常状态与资源释放正确组合；桥关闭不影响心跳。
3. 补可移植组合契约及Windows实际WebMessage用例。

## 必须交付

- 完整Launcher附件组合代码与待验Windows入口。

## 本任务验收

- 没有TODO/mock在生产路径代替Clipboard/WebView；Core与JS使用同协议。
- 交叉构建支持时必过，原生事件/渲染单独待验。

## 失败与停止条件

不能将未实现Windows适配记为仅待实机；代码缺失仍未完成。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
