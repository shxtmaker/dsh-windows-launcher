# D22 · 验证最终 tarball 的干净安装

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D21。默认下一建议：D23（仅建议，不在同轮自动执行）。

## 单一目标

证明交付包能在干净的全家桶环境运行，而非仅source link有效。

## 允许改动范围

- `插件最终构建与test:pack`
- `第二个隔离profile安装fixture`
- `包内容清单`

## 执行步骤

1. 对候选源码build并pack，检查host/client资源和依赖。
2. 第二个profile安装固定all和实际tarball，重跑代表性上传/草稿/发送闭环。
3. 生成包SHA-256、integrity、内容与版本清单。

## 必须交付

- 候选tarball、干净安装证据、脱敏manifest。

## 本任务验收

- 没有额外remote实例，包内不含凭据/测试私密数据。
- 实际运行的是本轮tarball的最终bundle。

## 失败与停止条件

不要以link安装代替包验证，不发布npm或修改生产profile。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
