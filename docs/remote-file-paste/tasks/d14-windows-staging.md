# D14 · 编写 Windows 文件快照与暂存适配

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D11。默认下一建议：D15（仅建议，不在同轮自动执行）。

## 单一目标

实现完整Windows文件暂存代码，开发验收仅针对代码和可移植约束。

## 允许改动范围

- `Platform.Windows/Attachments/`
- `相关Windows测试代码`
- `平台中立限额与所有权测试`

## 执行步骤

1. 只接受原生用户动作捕获的普通文件候选，拒绝UNC/目录/重解析绕过/云占位自动下载。
2. 受控快照、文件变化检测、磁盘限额、只清理自有目录并正确关闭句柄。
3. 写Windows用例但不声称运行；Linux检查生产所有权规则与可用的交叉编译。

## 必须交付

- 可审查的Windows暂存适配代码与待执行实机案例。

## 本任务验收

- 没有任意远端路径读取入口；失败清理不会触及应用目录外。
- 已发现源码错误修复；真实Windows文件属性/锁/ACL标WindowsPending。

## 失败与停止条件

无Windows不算blocked；确实需要系统事实则写最小待验项，不用stub代替生产适配。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
