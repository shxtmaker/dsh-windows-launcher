# D16 · 实现原生粘贴手势与剪贴板适配

轨道：DEV。状态以 `../execution/state.json` 为唯一真值；本任务卡不预置通过结果。

依赖：D14、D15。默认下一建议：D17（仅建议，不在同轮自动执行）。

## 单一目标

完整实现CF_HDROP/截图入口及唯一消费者，不让网页主动读取任意文件。

## 允许改动范围

- `Desktop/Attachments/ 原生输入与STA读取`
- `截图PNG适配`
- `Windows用例代码`

## 执行步骤

1. 活动窗口/标签/焦点和真实原生手势产生短期操作授权。
2. 纯文本保留原行为；同时存在文件列表与位图时优先文件列表。按页面握手确定 native-paste/bridge 模式，同一动作只有一个消费者，不因原生 paste 超时再次桥导入。
3. 无session/锁定/子代理提示，不自动创建会话；剪贴板busy和异常有可恢复结果。

## 必须交付

- 原生输入与截图代码、平台中立策略测试、手工验证步骤。

## 本任务验收

- 不信任页面isTrusted字段或路径；耗时复制不阻塞STA。
- 原生快捷键覆盖仍标WindowsPending，不把Linux PNG当截图采集已验。

## 失败与停止条件

若手势捕获API边界待实机，代码和安全策略照常完成；不全局截断所有Ctrl+V。

本轮只完成本任务及其直接验证/局部修复。达到验收、出现具体阻断，或到达需要拆分的范围边界时，写入轮次记录与检查点并结束；不得转入下一任务。缺少 Windows 实机仅影响独立系统验收，不是跳过开发实现的理由。

## 轮末证据与恢复点

记录变更文件、实际命令/退出码/用例数、证据路径与被测候选摘要；列出未验项及所属轨道。状态为 partial/blocked 时写明已保留成果、剩余一步和解除条件；下一轮先核对工作树及进程再恢复。
