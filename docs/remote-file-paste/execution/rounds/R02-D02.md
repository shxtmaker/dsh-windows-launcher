# R02 / D02

用户执行指令：“按建议继续”（并授权后续按建议连续推进，在阶段性任务完成后暂停汇报）。

方案版本与被测候选摘要：方案 2.0。候选为 Launcher 仓库工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c3f7a0a34d6ed4fdd7804e369bdd0258b`（未提交）。本轮为未提交的工作树候选。

本轮范围：仅 D02（建立可增量扩展的 Linux 开发门禁）。允许改动范围：`eng/verify-portable.ps1`、`eng/verification-profiles.json`、`eng/verification-tests.ps1`、`eng/verify.ps1`、`eng/common.ps1`。为满足 D02 验收「Windows 发布入口拒绝将 portable 摘要用作正式发布凭据」，另在 `eng/package.ps1`、`eng/package-internal.ps1` 接入结构断言并复用 `eng/common.ps1` 的新函数。

执行前状态：D01 改动仍在工作树；`eng/verify-portable.ps1` 等三个文件不存在；无遗留构建进程。依赖 D01 已 done。

实际变更：

| 文件 | 变更 |
| --- | --- |
| `eng/verification-profiles.json` | 新增。两个显式 profile（Core / Development）、六个项目的预期 TFM/RID 与可移植标记、可移植与完整 solution 结构期望、三个测试程序集的 Linux 可执行性与原因、MTP 枚举/执行参数、固定数据集路径、交叉构建状态取值、正式入口拒绝可移植摘要的声明 |
| `eng/verification-tests.ps1` | 新增。`Get-DshVerificationProfiles`（含可移植项目必须是 `net10.0`/无 RID 的护栏）、`Get-DshExpectedTestDataset`、`Get-DshTestAssemblyInventory`（按项目求值 RID 调用 runner 并枚举）、`Invoke-DshTestAssemblyCheck`（failSkips 执行 + 零用例/零跳过/零 notRun + 集合级固定数据集比对 + 触发标签格式核对 + 逐用例证据落盘） |
| `eng/verify-portable.ps1` | 新增。入口与摘要：环境检查、按 profile 顺序执行检查、`portable-verify-summary.json`、失败即非零退出 |
| `eng/common.ps1` | 新增 `Assert-DshWindowsVerifySummary`：按结构断言摘要来自 Windows 正式门禁，拒绝可移植摘要与非法形态 |
| `eng/package.ps1` | 打包前调用该断言（拒绝可移植摘要冒充发行验证） |
| `eng/package-internal.ps1` | 同上 |
| `eng/verify.ps1` | (a) 新增正式入口拒绝可移植摘要的结构检查；(b) 新增 `eng/verification-profiles.json` 在位与模式校验，并要求三个测试程序集与配置一致、可移植项目声明合法 |

未改动：`eng/expected-test-dataset.json`（固定基线未动，仅在负向测试中临时篡改并已还原）、`eng/release-constants.json`、全部生产源码与测试源码、`.gitattributes`。

实际验证（平台 Linux 7.0.0-30-generic x86_64，SDK 10.0.400；退出码均为实际执行结果）：

| # | 检查 | 结果 |
| --- | --- | --- |
| 1 | 六个 .ps1 解析检查（`verify.ps1`/`common.ps1`/`verification-tests.ps1`/`verify-portable.ps1`/`package.ps1`/`package-internal.ps1`） | 全部无解析错误 |
| 2 | `-Profile Core` 运行（含重复运行 3 次） | 退出码 0；7 项必需检查全 pass；`cases=49/49 failed=0 skipped=0`；12 项 Development 检查记 incomplete |
| 3 | `-Profile Development` 运行 | 退出码 0；9 项已实现检查全 pass（含 `cross-build-l12=pass`、`supply-chain-scan=pass`）；10 项未实现记 incomplete |
| 4 | 摘要 schema | `portable-verify-summary.json` 含 plan 第 6 节要求的全部字段；`portableStatus=fail`、`developmentReady=false`、`releaseEligible=false`、`harnessIntegration/productionInterop/windowsValidation=notRun` |
| 5 | **必需项缺失不被吞掉** | Core profile 在 6 项必需检查全 pass、49/49 用例通过的情况下，`portableStatus` 仍为 `fail`（12 项 incomplete），且明示「完整 Development 验证仍未完成」 |
| 6 | **零用例/数据集被改动即失败** | 临时删除固定数据集一条哈希并加入一条伪哈希 → `core-test-execution` 判 fail，报 `missing=1 unexpected=1`，退出码 1；数据集已还原（`git diff` 无差异） |
| 7 | **发布入口拒绝 portable 摘要** | 6 个用例的独立探针全部符合预期：拒绝真实可移植摘要、拒绝缺字段、拒绝非 PASS、拒绝错误 RID、拒绝缺失文件，且接受合法 Windows 形态摘要（证明断言非无条件拒绝） |
| 8 | **配置篡改即失败** | 把 Core 在 `verification-profiles.json` 中声明为 Windows 项目 → 入口报「可移植项目 DshLauncher.Core 的平台声明不合法」，退出码 1；配置已还原 |
| 9 | 输出洁净 | 修复返回泄漏后，两个 profile 输出中伪装成结果的裸 `pass`/`fail` 行为 0 |

证据路径：`artifacts/verify-portable/d02-evidence.log`（两个 profile 的完整输出与摘要）、`artifacts/verify-portable/portable-verify-summary.json`（最终摘要）、`artifacts/verify-portable/DshLauncher.Core.Tests.test-evidence.json`（逐用例证据）。`artifacts/` 已被 `.gitignore` 排除。

本轮修复的三处自身缺陷（均为门禁正确性缺陷，已在轮内修复并重跑）：
1. `Invoke-DshTestAssemblyCheck` 的 `[System.Collections.Generic.List[string]] $Failures` 缺 `[AllowEmptyCollection()]`，空集合无法绑定，导致首个必需检查恒失败。
2. 固定数据集比对最初用 `-join '|'` 与 `Compare-Object` 位置绑定，产生假差异（报 missing=1/unexpected=1 但实际 49 条完全一致，且 `Compare-Object` 把整数组当成单个对象打印）。改为 `HashSet.SetEquals` 与 `ExceptWith` 的集合语义。
3. `Invoke-DshPortableStep` 的返回值泄漏到输出；且在脚本块内给父作用域变量赋值不生效，导致 `cross-build-l12` 实际 pass 却记为 `fail`。改为由函数返回状态、调用方接收。

未执行项与所属轨道：

- Development profile 中 10 项检查为 incomplete：`plugin-typecheck`、`plugin-build`、`plugin-unit-tests`、`plugin-browser-tests`、`plugin-integration-suite`、`plugin-pack`（属 D03–D12 的插件工作）、`host-fixture-minimal-loop`（D04–D09）、`production-interop-l13`（D18）、`final-tarball-install`（D22）、`resource-and-failure-suites`（D19–D21）。摘要中显式记 incomplete 并置 `portableStatus=fail`，未冒充通过。
- `eng/verify.ps1` 完整运行：仍属 Windows 轨道（W-01），本机不可执行。
- 两个 Windows 测试程序集的 Linux 执行：属 WindowsPending，`verification-profiles.json` 已记录不可执行原因，记 notRun。
- 未执行发布、打包、安装、远端推送。

本轮结果：done。

D02 验收对照：
- Core 格式/构建/实际测试可重复通过 ✅（第 2 项，重复 3 次结果一致）
- 必需项缺失、零用例、一般编译错误和恢复失败均不能被摘要吞掉 ✅（第 0 项：必需项缺失 → portableStatus=fail；第 6 项：数据集变动 → 退出码 1）
- Windows 发布入口拒绝将 portable 摘要用作正式发布凭据 ✅（第 7 项 6 用例 + `eng/verify.ps1` 内联结构检查）
- 原 verify 的公共 props、统一 win-x64 runner、锁文件数量断言已按显式配置调整 ✅（D01 已完成公共 props 断言改造；本轮锁文件数量断言进入 `verification-profiles.json` 并由 `verify-portable.ps1` 核对、`verify.ps1` 核对三程序集与配置一致；`verify.ps1` 的 runner 调用在 D01 已按项目求值 RID）
- 不把所有 Windows 项 skipped 后宣布完整验证通过 ✅（本机 profile 只有 Core 必需，Windows 项记 notRun 且 `developmentReady=false`）

已保留成果：可重复运行的 Core profile 门禁、Development profile 检查清单与结果 schema、发布入口的 portable 摘要拒绝护栏，以及上述证据文件。

剩余步骤或解除条件：无。D02 无阻断。Development profile 的 10 项 incomplete 由后续 DEV 任务逐项登记实现，符合「D03–D23 各任务完成时同步注册该任务的检查与证据」的增量要求。

本轮仍运行的后台写入：无。所有构建与测试同步结束，无遗留 dotnet/MSBuild 进程。

下一轮建议：仅建议，不自动执行。D02 完成后，D03（依赖 D00）成为编号最小且依赖已满足的 todo 任务。按用户授权继续推进 D03：建立 `plugins/dsh-remote-attachments/` 独立附加插件骨架与最小包（自有包身份、host/client/shared 分层、tarball 含运行入口与必要资源、不含凭据或生产数据）。启动真实 Harness＋全家桶夹具的 D04 依赖 D03，因此 M2 的最小闭环须先完成插件骨架。
