# R28 · D25 · 冻结开发交付并移交 Windows 实机验证

方案版本 2.0。候选：工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c3f7a0a34d6ed4fdd7804e369bdd0258b`（未提交，dirty 37）。

本轮任务：D25。状态 **done**：全部 DEV 任务与证据核对有效，源码/tarball/Windows 交接齐备 ⇒ 项目级 **`DevelopmentReady`** 记账，**Windows 保持 pending**，**开发调度结束**。

## 1. 候选绑定（步骤 1）

- gitHead `df68795c3f7a0a34d6ed4fdd7804e369bdd0258b`；分支 `feat/remote-file-paste-attachments`；dirty **37**；`git status --porcelain=v1`（Trim 后）`sha256=0b99fd98968999bb5173eea6c1865e9f614df27d7e12a2045beaf616afdc8c10`。
- 插件 `@shxtmaker/dsh-remote-attachments@0.1.0`；`src` 树 18 文件 `sha256=c4597fe4e012c76571c8cde8211b23b0aef6698b8ffc366d4c8d8ae78b0dfbbe`。
- **与 D24 报告 `candidate` 逐字相同** ⇒ 候选未变，D24 权威轮（runId `d24-2026-09-15T11-15-43-616Z-0a0f006c`，32/32 pass）证据**仍有效**，无需回退重验。
- 锁：6 份 `packages.lock.json`、插件 `pnpm-lock.yaml`、`compatibility-lock.json` 逐个 SHA-256 记入交接清单。
- tarball：`sha256=f8961215a8c9dffcca44ff78b82a49e8f39708e5df59e84bad46a161b0bc9556`，163 832 B / 59 文件。
- **无自动正式版本升级**：`eng/release-constants.json` 的 `version=2.0.9`、`releaseStatus=candidate` 未改；未打标签、未提交、未推送、未发布。

## 2. Windows 输入材料与交接件（步骤 2 + 必须交付）

| 交付件 | 路径 | 内容 |
| --- | --- | --- |
| 交接清单 | `artifacts/verify-portable/d25-handoff-manifest.json` | 候选/锁/上游身份/tarball/Linux 证据/待验清单/本轮复核 一次绑定 |
| Windows 运行手册 | `docs/remote-file-paste/execution/windows-runbook.md` | 材料表、工具依赖、源码待编译项、原生用例段、W00–W06 步骤与命令、安装与回退、证据与脱敏、已知问题 |
| Windows 任务列表与待验清单 | `docs/remote-file-paste/execution/windows-task-list.md` | W00–W06、W-01…W-08.x、WP/WW/WS/WB/WM 全量编号、20 条门禁级 notRun、W00-A/B 决策项 |
| 交付报告（冻结） | `docs/remote-file-paste/execution/delivery-report.md` | D25 终态 + Windows 轮次必须先看的问题 |
| 开发交接（恢复入口） | `docs/remote-file-paste/execution/handoff.md` | 终态、交付件、恢复入口、实测事实与陷阱、口径纪律 |
| tarball + manifest | `plugins/dsh-remote-attachments/pack/*.tgz`、`artifacts/verify-portable/d22-package-manifest.json` | 包内 59 文件清单与运行期字节证明 |

Windows 侧覆盖：工具依赖（SDK 10.0.400 / pwsh7 / Node22+ / pnpm / Chromium / WebView2 SDK 与 Runtime / Inno 7.0.2）、
6 个项目的 TFM+RID 与"哪些只在 Windows 编译/执行"、固定用例基线（Core 546 / Acceptance 57 / Platform.Windows 67，removed=0）、
xunit 64 条 + 手工 40 条原生用例、Offline/Online 安装与功能/版本回退方案。

## 3. 核对全部 DEV 任务与证据（步骤 3）

- DEV 叶任务 **D00–D24 全部 done（25 项）** + R15 修复轮 done；无 `partial`/`blocked`；轮次记录 R00–R27 共 28 份。
- **没有任何 Windows 平台证据被记 `pass`**（逐条审计 state.json 的 `evidence[].platform` 与 `result`）。
- 生产源码无 `TODO`/`FIXME`、无 stub 抛壳、无 `#if` 假实现；未完成标记 **0**。
- 证据路径审计：仅 1 个临时夹具目录 `artifacts/fixture/provider/record/` 不存在——夹具按设计在 `down.sh` 清理根目录，
  门禁运行期内会重建；其余证据路径全部存在。
- **判定：`developmentStatus=DevelopmentReady`**（依据 [implementation-plan §7](../implementation-plan.md)：全部 DEV 叶任务完成且证据有效 +
  D24 完整 Linux 门禁真实通过 + D25 源码/tarball/实机交接完整，不需要 Windows 在线）；`windowsStatus=pending`；`releaseEligible=false`。

## 4. 本轮实际命令与结果（父代理亲测）

| 命令 | 退出码 | 结果 |
| --- | --- | --- |
| 独立重算 porcelain 摘要与插件 `src` 树指纹 | 0 | 与 D24 `candidate` 逐字相同 |
| `pnpm run build`（插件）+ 解包 tarball 逐文件比对 | 0 | **59/59 字节相同**；sha256 与 D22 清单一致 |
| 逐字提取 `eng/verify-portable.ps1:76-148` 三个纯函数 + 变异输入 | 0 | **18/18** 控制成立（含零用例/跳过/未验/失败/计数不符/独立性/`toolchainUnsupported`/WindowsPending 不误报） |
| 脚本副本 A：正式摘要落盘后注入失败（守卫保留） | 1 | 摘要**未被覆盖**：`requiredCases=542`、保留 candidate、无 `failure` 字段；Core 7/7 pass |
| 脚本副本 B：同注入但关闭守卫（负向对照） | 1 | 摘要**确实被覆盖**：`requiredCases=0`、无 candidate、有 `failure` 字段 ⇒ 测试非空转、D24 第 3 处修复真实生效 |
| `pwsh -File eng/verify-portable.ps1 -Profile Core`（副本 A 顺带复跑） | （注入致 1） | Core 检查 7/7 pass；Core 用例 **542/542**（failed/skipped/notRun 均 0） |
| state.json / 证据路径 / Windows 误记审计 | 0 | 见 §3；Windows 误记 0、未完成标记 0 |

副本与 `/tmp` 工作目录已删除；`git status --porcelain` 回到 **37 行**（Trim 后摘要与 D24 相同）；无遗留后台进程。

## 5. 证据路径

- `artifacts/verify-portable/d25-handoff-manifest.json`（本轮新增，D25 权威绑定）
- `artifacts/verify-portable/portable-verify-summary.json`（D24 权威轮摘要，本轮未改动）
- `artifacts/verify-portable/d24-linux-report.json`、`d24-runs/`
- `docs/remote-file-paste/execution/{windows-runbook.md,windows-task-list.md,delivery-report.md,handoff.md}`
- `docs/remote-file-paste/execution/rounds/R28-D25.md`（本文件）

## 6. 未验 / 不确定（按轨道）

- **Windows 轨道（WindowsPending，不计通过）**：全部实机事实与正式门禁——两个 Windows 项目/测试程序集执行、
  WPF/WebView2/Win32 语义、`eng/verify.ps1` 的 `verify-summary.json`、Offline/Online 正式打包与安装、升级与回退、RS-01…RS-15。
  明细见 [windows-task-list.md](../windows-task-list.md) §3–4（13 条 `WindowsPending` + 7 条白名单受限）。
- **本轮未重跑完整 Development 门禁**：候选未变化，D24 权威轮证据仍绑定同一工作树；本轮只做 Core profile 复跑、
  纯函数级控制与摘要保护级端到端证伪。若候选在 Windows 轮次被修改，必须重跑受影响的 Linux 项。
- 检验方法边界（如实记录）：摘要保护测试用的是**脚本副本**（原脚本逐字复制 + 注入），不是原文件；Core profile 的
  542 是 core 集合（546 含 interop 4），二者口径不同。

## 7. 口径纪律（写入交接，后续必须遵守）

- `portableStatus=pass` **不等于**"所有测试通过"，更**不等于可发布**；`releaseEligible=false` 恒定。
- `portable-verify-summary.json` 的 `developmentReady=false` 是 **profile 配置**（`producesDevelopmentReady=false`），
  与项目级 `DevelopmentReady` 是两个口径；正式发布门禁摘要只能是 Windows `eng/verify.ps1` 的 `verify-summary.json`。
- `Session.promptError` 公开路径存在（插件 `ctx.sessions.scope(id)` → `sessionOf(scope)` → `getSnapshot().promptError`）；
  当前黑盒测试**没有插件上下文句柄**，不得说成"没有公开接口"。
- 撤销证据只表述为"请求到达 Host 前被门控拒绝"（403 + `unpaired`），不得扩展为"已通过鉴权的上传流被主动终止"。
- 不自动启动 Windows 阶段、正式打包/安装、推送、Release 或 npm 发布。
- 交付报告更新纪律：只在出现需要 Windows 轮次处理的问题时更新；本轮属该情形（开发收尾 + Windows 移交 +
  反斜杠基线隐患），故一并冻结终态。

## 8. 状态

D25 **done**。DEV 轨道全部任务完成，项目记 **`DevelopmentReady`**，Windows 保持 **`pending`**，`releaseEligible=false`，
**开发调度结束**。下一轨道为 Windows 实机验证（W00–W06），需用户明确指令启动；本任务不启动、不打包、不发布。
