# R27 · D24 · 收敛完整 Linux 开发验证

方案版本 2.0。候选：工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c3f7a0a34d6ed4fdd7804e369bdd0258b`（未提交，dirty 37）。

本轮任务：D24。状态 **done**：在**干净状态**下对同一候选跑完整必需开发检查，**32/32 checks pass、incomplete 0、fail 0**。

## 1. 权威运行（唯一一轮）

```bash
bash plugins/dsh-remote-attachments/tests/fixtures/down.sh   # 干净状态
pwsh -NoProfile -File ./eng/verify-portable.ps1 -Profile Development -ArtifactsDirectory ./artifacts/verify-portable
```

- **runId `d24-2026-09-15T11-15-43-616Z-0a0f006c`**（11:15:43Z → 11:42:04Z，约 26 分钟）
- **32/32 checks pass、incomplete 0、fail 0、exit 0**
- `portableStatus=pass`、`crossBuildStatus=pass`（**独立**）、`requiredCases=546 / executedCases=546`（failed=0 skipped=0）
- `harnessIntegration=pass`、`productionInterop=pass`、`windowsValidation=notRun`
- **`developmentReady=false`**（profile 配置使然）、**`releaseEligible=false`**（恒定）

产物：正式摘要 `artifacts/verify-portable/portable-verify-summary.json`；机器可读报告 `artifacts/verify-portable/d24-linux-report.json`（+ `d24-runs/<runId>/report.json` 与只追加 `d24-runs/index.json`）；原始输出 `d24-run-console.log`；L12 构建日志 `d12-cross-build.log`；草案报告 `plugins/dsh-remote-attachments/docs/D24-LINUX-REPORT.md`。

## 2. L 覆盖表（L01–L11 + L13 全部 pass，**缺口 0**；L12 单独判定）

| 层 | 实现的检查 |
| --- | --- |
| L01 | platform-evaluation / portable-solution-shape / architecture-constraints / core-locked-restore / core-format / core-build |
| L02 | core-test-execution（542） |
| L03 | wire-contract-goldens |
| L04 | browser-receiver-fixture / plugin-unit-tests |
| L05 | client-composer-fixture / draft-adapter-fixture |
| L06 | host-fixture-bundle / addon-bridge-fixture / addon-compatibility-fixture |
| L07 | upload-hook-fixture / provider-fixture / host-fixture-minimal-loop |
| L08 | plugin-integration-suite |
| L09 | pairing-fixture / pairing-fixture-https / resource-and-failure-suites |
| L10 | supply-chain-scan / plugin-pack / final-tarball-install |
| L11 | plugin-browser-tests / resource-and-failure-suites |
| **L12** | **cross-build-l12 = pass（独立判定）** |
| L13 | production-interop-l13 |

每层的证据路径见 d24 报告的 `layers[].evidence`（含**存在性 + 本轮新鲜度**）。

## 3. 零容忍与独立性（均带**可证伪**负向控制）

- **零容忍（7/7 成立）**：`requiredCases/executedCases ≤ 0`、执行数 ≠ 必需数、`failed ≠ 0`、`skipped ≠ 0`（含 notRun）、任一必需层非 pass、任一用例证据计数 0、**notRun 无理由** ⇒ 产生违规并令 `portableStatus=fail`。负向控制 **C1(0 用例) / C2(skip) / C3(notRun) / C4(层 incomplete) / C5(无理由 notRun)** 均**确实失败**；C0/C6 证明干净输入与 WindowsPending 输入**不误报**。
- **独立性（2/2 成立）**：`Resolve-DshPortableStatus` 的参数表**没有** `CrossBuildStatus`（**结构性隔离**），可移植失败集合显式排除 `cross-build-l12`。I1：单独把 cross-build 改 fail ⇒ `portableStatus` 仍 pass、`crossBuildStatus` 变 fail；I2：单独把 core-build 改 fail ⇒ `portableStatus` 变 fail、`crossBuildStatus` 仍 pass。交叉构建失败仍经 `$failures` 令进程 `exit 1`（**不静默**）。

## 4. 候选一致性

gitHead `df68795c…`、分支 `feat/remote-file-paste-attachments`、dirty 37（porcelain sha256 `0b99fd98…`）、插件 v0.1.0、**src 树 `c4597fe4e012c76571c8cde8211b23b0aef6698b8ffc366d4c8d8ae78b0dfbbe`**（18 文件）。d19/d20/d21/d22 的内嵌 candidate **逐字段一致**；全部 artifacts 证据 mtime 在本轮窗口内；D22 manifest ↔ gates 的 tarball sha256 一致。**身份违规 0**。d10/d12/d13/d18/d23 证据未内嵌 candidate（d18 由 C# 写、不在改动范围）⇒ 已在报告 `identityNotDeclared` **显式列出**，并用"同轮重写"兜底，不当作通过。

## 5. notRun 清单（合计 20，**unjustified = 0**）

- **WindowsPending 13**：两个 Windows 测试程序集、两个 Windows 项目、`WindowsValidationAndDevelopmentReady`、D21 的 WP-01..03、D22 四项、D23 的 WindowsPackagingAndUpdate。
- **显式白名单受限场景 7**：D20 的 G21b/G33/G38；D23 的 RealAllBundleUpdate / UnverifiedUpstreamCombination / MarketplaceUpdateFlow / RealIndependentScopePluginUpdate。
- **规则默认拒绝**：非 WindowsPending、非白名单者一律判 `unjustified` 并**失败**。

## 6. 顺带修掉四处门禁缺陷（均在 `eng/verify-portable.ps1`）

1. **`portableStatus` 原取"所有 fail 检查"** ⇒ 交叉构建失败会连带把它打成 fail（**互相掩盖**）；现只取**可移植失败集合**。
2. **交叉构建原先不落日志** ⇒ L12 没有证据；现捕获 stdout/stderr 写入 `d12-cross-build.log`。
3. **真实门禁缺陷（最重要）**：任何检查失败时 `if ($failures.Count -gt 0)` 的 `Write-Error` 在 `$ErrorActionPreference='Stop'` 下抛**终止错误**，被外层 `catch` 用**兜底摘要覆盖正式摘要**（cases 全 0、丢 candidate/testAssemblies）。这解释了此前几轮我看到的"摘要像旧值/形状异常"的现象，**实测于第一次 D24 失败轮**。已加 `$summaryWritten`，`catch` **仅在未落盘时**兜底。
4. **摘要 `harnessIntegration` 原硬编码 `notRun`**，与 L06/L07 六项真实 Harness 全 pass **矛盾（虚报）**；现由 `Resolve-DshHarnessIntegrationStatus` 推导（本次 = `pass`）。

## 7. 未验 / 不确定

- **Windows 实机全部 notRun**：WPF/WebView2/MSI、`eng/verify.ps1` 的正式 DevelopmentReady。L12 是 **pass 而非 `toolchainUnsupported`**。
- `$HOME/.dsh` 未被触碰：D22 G22 / D23 G13a 均 `before == after` 通过，运行窗口内 `~/.dsh` **零文件改动**；但按**全树**算的粗指纹会因**用户自己的 DSH 运行时**持续写 `task-board/`、`dsh-usage/`、`profiles/sdk/cordis.yml` 而漂移（mtime 在运行窗口之外/之后），**不是门禁行为**。
- 我（父代理）需要复核的点见交接：`portableStatus` 与 `crossBuildStatus` 的独立派生、零容忍断言的负向控制、以及第 3 条摘要覆盖缺陷的修复是否真的生效。

## 8. 状态

D24 **done**：干净环境下 **32/32 checks 全 pass**、L01–L11+L13 覆盖缺口 0、L12 独立判定为 pass、零容忍与独立性均有可证伪负向控制、候选身份违规 0、notRun 全部正当（unjustified 0）；顺带修掉四处门禁缺陷（含**摘要被兜底覆盖**这一会造成假象的严重缺陷）。**口径纪律**：`portableStatus=pass` **不等于**"所有测试通过"，更**不等于可发布**；`releaseEligible=false` 恒定；Windows 实机全部 WindowsPending。D25 依赖解除。
