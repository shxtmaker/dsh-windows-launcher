# R01 / D01

用户执行指令：“按建议继续”。按分轮规则恢复上轮建议的就绪任务 D01，不同轮并行开发其他任务。

方案版本与被测候选摘要：方案 2.0。候选为 Launcher 仓库工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c3f7a0a34d6ed4fdd7804e369bdd0258b`（未提交，改动留在工作树）。工作树内容摘要（`git diff` + `git status --porcelain` 的 SHA-256 前 16 位）：`8621f47bb59f87e3`。本轮为**未提交的工作树候选**，非提交候选。

本轮范围：仅 D01（分离 Core 的平台目标）。D01 允许改动范围：`Directory.Build.props`、六个 `.csproj` 与相关 lock、新增 `DshWindowsLauncher.Portable.slnx`。为让 D01 的直接验收成立，另按方案第 2.2 节调整了 `eng/verify.ps1` 与 `eng/common.ps1` 中依赖公共 props 字符串的 TFM/RID 断言（方案明确要求“改为核对各项目最终 MSBuild 求值”）。未开始 D02，未建 `eng/verification-profiles.json`、`eng/verification-tests.ps1`、`eng/verify-portable.ps1`。

执行前状态：工作树仅含 R00 新增的 `docs/remote-file-paste/`；HEAD 与 R00 相同；无遗留 dotnet/msbuild 进程；无构建产物。依赖 D00 已 done。

实际变更：

| 文件 | 变更 |
| --- | --- |
| `Directory.Build.props` | 移除统一的 `TargetFramework`/`RuntimeIdentifier`/`PlatformTarget`/`EnableWindowsTargeting`，加注释说明平台身份改由各项目显式声明；保留语言版本、分析器、警告即错误、确定性、锁文件、NuGet 审计与发行身份导入 |
| `src/DshLauncher.Core/DshLauncher.Core.csproj` | `+<TargetFramework>net10.0</TargetFramework>`（无 RID） |
| `tests/DshLauncher.Core.Tests/DshLauncher.Core.Tests.csproj` | `+<TargetFramework>net10.0</TargetFramework>`（无 RID） |
| `src/DshLauncher.Platform.Windows/…csproj` | 显式 `net10.0-windows` + `win-x64` + `x64` + `EnableWindowsTargeting` |
| `src/DshLauncher.Desktop/…csproj` | 同上 |
| `tests/DshLauncher.Platform.Windows.Tests/…csproj` | 同上 |
| `tests/DshLauncher.Acceptance.Tests/…csproj` | 同上（UTF-8 BOM 已保留） |
| `DshWindowsLauncher.Portable.slnx` | 新增；只含 Core 与 Core.Tests |
| `src/DshLauncher.Core/packages.lock.json` | 显式重建：TFM 键 `net10.0-windows7.0`(+`/win-x64`) → `net10.0` |
| `tests/DshLauncher.Core.Tests/packages.lock.json` | 显式重建：同上；24 个包与版本/哈希/依赖全部不变 |
| `eng/common.ps1` | 新增 `Get-DshProjectPlatform`：用 `dotnet msbuild -getProperty:` 读取项目最终求值的 TFM/RID/PlatformTarget |
| `eng/verify.ps1` | (a) 删除对公共 props 中 `<TargetFramework>net10.0-windows</TargetFramework>`、`<RuntimeIdentifier>win-x64</RuntimeIdentifier>` 字面片段的断言，改为按六个项目逐一核对最终 MSBuild 求值；(b) 新增可移植 solution 只含两项目的结构断言；(c) 三个测试程序集的 `--runtime` 改为按项目求值 RID 决定（Core.Tests 不再被强制 `win-x64`）；(d) 门禁标签 `release-win-x64-build` → `release-solution-build` |

未改动：`eng/release-constants.json` 及其 schema（正式发行身份仍是 `net10.0-windows` + `win-x64`，`eng/common.ps1` 第 275–282 行断言保持原样）、`eng/expected-test-dataset.json`、`eng/package*.ps1`、`eng/release-smoke.ps1`、全部生产源码与测试源码。

实际验证（平台 Linux 7.0.0-30-generic x86_64，SDK 10.0.400；全部退出码来自实际执行）：

| # | 检查 | 命令 | 退出码 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | 六项目平台求值 | `dotnet msbuild <proj> -getProperty:TargetFramework\|RuntimeIdentifier\|PlatformTarget -nologo` | 0 | Core `net10.0`/空/空；Core.Tests `net10.0`/空/空；其余四项 `net10.0-windows`/`win-x64`/`x64` |
| 2 | locked restore（可移植子集） | `dotnet restore DshWindowsLauncher.Portable.slnx --locked-mode` | 0 | 2 项目还原成功 |
| 3 | locked restore（完整 solution） | `dotnet restore DshWindowsLauncher.slnx --locked-mode` | 0 | 6 项目全部就绪 |
| 4 | 格式（可移植子集） | `dotnet format DshWindowsLauncher.Portable.slnx --verify-no-changes --no-restore` | 0 | 无差异 |
| 5 | 格式（完整 solution） | `dotnet format DshWindowsLauncher.slnx --verify-no-changes --no-restore` | 0 | 无差异 |
| 6 | 构建（可移植子集，Release，warnaserror） | `dotnet build DshWindowsLauncher.Portable.slnx -c Release --no-restore --warnaserror` | 0 | 0 警告 0 错误；输出 `net10.0/DshLauncher.Core.dll`、`net10.0/DshLauncher.Core.Tests.dll` |
| 7 | 构建（完整 solution，L12 交叉构建） | `dotnet build DshWindowsLauncher.slnx -c Release --no-restore --warnaserror` | 0 | 六个项目全部生成，0 警告 0 错误（Windows 四项输出仍在 `net10.0-windows/win-x64`） |
| 8 | Core 用例枚举 | `dotnet run --project tests/DshLauncher.Core.Tests/… -- -list full/json -preEnumerateTheories …` | 0 | **枚举 49 例**，与固定数据集 49 条一致 |
| 9 | Core 用例实际执行 | 同上 + `-explicit on -failSkips -printMaxStringLength 0 -reporter quiet -result-xml artifacts/verify-portable/core-results.xml` | 0 | **total=49 passed=49 failed=0 skipped=0 notRun=0**（0.349 s）；实际用例 SHA-256 集合与 `eng/expected-test-dataset.json` 逐条相同 |
| 10 | 锁文件差异核对 | 与 `HEAD` 版本做 JSON 结构比对 | 0 | 两个 lock 仅 TFM 键变化；包增删 0、版本/哈希/依赖变化 0 |
| 11 | 架构约束 | 直接调用 `Get-DshArchitectureViolations` | 0 | 违规 0（Core 无 ProjectReference/PackageReference/FrameworkReference、无 WPF、无禁止命名空间；固定引用图成立） |
| 12 | PowerShell 语法 | `[Parser]::ParseFile` 解析 `eng/verify.ps1`、`eng/common.ps1` | 0 | 均无解析错误；`Get-DshProjectPlatform` 可解析 |
| 13 | 新增门禁块可失败性 | 从 `eng/verify.ps1` 提取平台核对块后执行正向与篡改期望值负向 | 0 / 期望抛出 | 正向通过；负向正确抛出“项目 DshLauncher.Core 的平台身份偏离可移植基线…”（临时探针已删除） |
| 14 | 门禁在改动前能捕获旧状态 | `dotnet restore … --locked-mode`（改 TFM 后、重建 lock 前） | 1 | 正确报 `NU1004`：RID 由 `win-x64` 变为空、锁定文件缺 `net10.0` 目标 |

证据路径：`artifacts/verify-portable/d01-evidence.log`（完整命令与退出码）、`artifacts/verify-portable/d01-core-enumeration.json`（49 例枚举）、`artifacts/verify-portable/core-results.xml`（49/49 结构化结果）。`artifacts/` 在 `.gitignore` 中，为本地证据不入库。

未执行项与所属轨道：

- `eng/verify.ps1` 完整运行：未执行。它是 Windows 正式门禁（要求 `-DotNetPath` 指向 Windows 精确 SDK、Release `win-x64` 原生构建与三个程序集原生测试），不在 Linux D01 范围。属 **Windows 轨道（W-01）**。
- `eng/verify-portable.ps1`、`eng/verification-profiles.json`、`eng/verification-tests.ps1`：尚未实现，属 **DEV/D02**。
- 两个 Windows 测试程序集的实际执行：本机实测边界已确证，属 **WindowsPending**——
  - `DshLauncher.Platform.Windows.Tests`：可直接加载执行，25 例中 10 通过、15 失败（失败类 `ApplicationDataStoreTests` 7、`JsonTargetStoreTests` 5、`CurrentUserSingleInstanceTests` 3），失败为真实 Win32 API（`CreateFileW` 等）在 Linux 不可用，是跨平台边界而非源码缺陷；用 `dotnet run --runtime win-x64` 调用时因缺 `Microsoft.WindowsDesktop.App 10.0.0` 而无法启动。
  - `DshLauncher.Acceptance.Tests`：无法执行，`dotnet` 直接报缺 `Microsoft.WindowsDesktop.App 10.0.0`（退出码 150）。
  - 两者均记 notRun（Linux 侧），不计为通过，也不构成本任务阻断。
- D01 未要求、也未执行：发布/打包、安装、远端推送、生产 profile 变更。均未发生。

本轮结果：done。

D01 验收对照：
- Core/Core.Tests 最终 TFM=`net10.0`、RID 为空 ✅（第 1 项实测）
- 生产 Core 不增加平台依赖 ✅（第 11 项：无任何 Reference、无 WPF、无禁止命名空间）
- 原有 Core 用例实际执行 ✅（第 9 项 49/49，非零用例、无跳过、无 notRun）
- 未说明的基线删除 ✅ 不存在：固定数据集 49 条逐条匹配，未改动 `eng/expected-test-dataset.json`
- 另四项目保持 Windows 目标 ✅（第 1、7 项）
- 正式发行身份未被削弱 ✅（`eng/common.ps1` 的 `release-constants` 断言保持原样，本机未运行该门禁）
- 保留 SDK/MTP/分析器/锁文件规则 ✅（`Directory.Build.props` 共享规则未动；`global.json` 未动）

已保留成果：可移植的 Core 与 Core.Tests、可移植子集 solution、按项目求值的门禁断言、重建后的两个 lock，以及上述证据文件。

剩余步骤或解除条件：无。D01 无阻断。Windows 实机运行两个 Windows 测试程序集属独立轨道，不阻断 D01。

本轮仍运行的后台写入：无。本轮启动的构建与测试均已同步结束，无遗留后台作业或进程。

下一轮建议：仅建议，不自动执行。D01 完成后 D02（依赖 D01）与 D03（依赖 D00）依赖均已满足，其中 D02 编号最小。建议下一轮执行 D02：建立可增量扩展的 Linux 开发门禁 `eng/verify-portable.ps1`（先提供 `-Profile Core`），按方案第 2.2 节新增 `eng/verification-profiles.json` 与 `eng/verification-tests.ps1`，使 Core 格式/构建/实际测试可重复通过；注意其通过不得输出 DevelopmentReady。Windows 实机验收、安装、发布与推送仍需用户另行明确授权。
