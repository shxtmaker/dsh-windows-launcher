# R23 · D20 · 会话与来源隔离

方案版本 2.0。候选：工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c…`（未提交）。本轮把门禁里长期 `incomplete` 的 `plugin-integration-suite` 实现为真。

本轮任务：D20。状态 **done**。**Development 未覆盖项 3 → 2**（剩 `plugin-browser-tests`、`final-tarball-install`）。

## 0. 本轮先经历一次失败派发（如实记录）

首次派发的实现子代理**中途失败且产物为零**（无 fixture、无证据、`eng/` 未被改到一半），环境无残留进程，等于白耗一轮。已重新派发，并在任务书里加了"开工先清理 + **增量落盘**，宁可交付诚实的不完整结果也不要什么都没有"。第二次尝试正常完成。

## 1. 交付

| 项 | 内容 |
| --- | --- |
| 新门禁 | `tests/fixtures/d20-isolation-gates.mjs`（npm 脚本 `fixture:isolation`） |
| D15 规则的 JS 逐条移植 | `tests/fixtures/d20-origin-policy.mjs`（对 `RemoteOrigin.cs`/`RemotePageOriginPolicy.cs`；30 向量 × 2 主机形态全绿） |
| 隔离矩阵 | `artifacts/verify-portable/d20-isolation-matrix.json` + 每次运行各一份副本 |
| 证据 | `artifacts/{verify-portable,fixture/evidence}/d20-isolation-gates.json` |
| 门禁接入 | `eng/verify-portable.ps1` 新增 `plugin-integration-suite` 步骤（唯一 eng 改动；`verification-profiles.json` 原本已有该 id，`verification-tests.ps1` 无需改） |

## 2. 隔离矩阵（验收核心）

**形状：12 操作 × 4 落点单元 = 48 行**，每行含 `determinate / outcome / code / landedIds / documentInstance / role`。单元 = `A×s1` / `A×s2` / `B×s1` / `B×s2`（A=3099，B=3097，**同一 `DSH_HOME`** 故两 target 看得到同一批会话；另有 4 个影子文档一并参与扫描）。**落点只认原生 `attachmentIds`**（探针导入的 `previous` 回读），绝不用 DOM 卡片。

落点扫描：`A×s1=3`、`B×s1=2`、`B×s2=0`、`A×s2=0`、4 个影子=0；账本 5 条交付全部被解释；**误投递 = 0（落错单元 0 / 账本不自洽 0 / 无归属 0）**。**扫描不完整时 `misDeliveryTotal=null` 且 G90 直接失败**——不允许"读不到"冒充 0。

逐操作（refused / imported / no-delivery / 落点 id / 确定码 / 误投递）：

| 操作 | 结果 |
| --- | --- |
| session-switch | 4/0/0/0 `context-changed` · 误投递 0 |
| page-refresh | 4/0/0/0 `no-session` · 0 |
| target-deletion | 0/0/4/0 — · 0 |
| hidden-page | 0/1/3/1 — · 0 |
| iframe | 0/0/4/0 — · 0 |
| cross-origin-navigation | 4/0/0/0 `no-session` · 0 |
| expired-late-messages | 4/0/0/0 `context-changed` · 0 |
| replay | 0/1/3/1 — · 0 |
| no-session-home | 4/0/0/0 `no-session` · 0 |
| locked-composer | 0/1/3/1 `file-input-disabled` · 0 |
| subagent-state | 4/0/0/0 `context-changed` · 0 |
| unknown-protocol-version | 4/0/0/0 `version-mismatch` · 0 |

## 3. 无泄漏

三类 canary 一起查：**本地绝对路径**（fixtureRoot/pluginRoot/repoRoot/dshHome/home、`/attachments/v1/files/`）、**文件正文**（唯一 marker 原文 + base64 前缀）、**凭据形态**（复用 `findSecretLeaks`）。覆盖 **47 个"可说出口"**：拒绝 detail、**出站报文**（每条 wire 消息）、状态面（`__DSH_ATTACHMENTS_ADDON__.status()` + `__DSH_ATTACHMENTS_STATUS__`）、页面可见文本（alert + bodyText）、页面全局。**命中 0**；canary 证明检查确实会响（路径/字节/凭据三类全中）；四个单元的可见面全部读到。落盘文本一律过 `redact`，报告自身也过 `findLeaks`。

## 4. 复验（本机实跑，我独立核对）

| 项 | 结果 |
| --- | --- |
| D20 判据 | **43/43 gates、13/13 负向探针、exit 0**（我核对：`result=pass`、`failedGateIds=[]`、43 条） |
| 隔离矩阵 | **48 行、`misDeliveryTotal=0`**（我核对） |
| eng 接入 | `plugin-integration-suite` 已进 `eng/verify-portable.ps1`（我核对 1 处）；步骤体单独执行 `ENG-STEP: PASS` |
| 回归 | `d13-bridge-gates` 16/16、`d19-failure-gates` 32/32、`pnpm run verify` exit 0 |
| 证据布局 | `d20-runs/index.json` **只增不改，4 次历史全留**（fail, fail, pass, pass）——我核对确认历史未被覆盖 |
| Development profile | 见 state.json |

**负向控制 13 条全部成立**（矩阵判定器对故意改错的账本报误投递 >0；清空账本报"无归属 id"；探针读数非空；刷新/删页前确有在途字节；异来源框架以 `SecurityError` 被证实；D15 正向对照；降级页确实加载；重放方向钉死；受限解除后同路径可用；泄漏 canary 三类命中；被停 target 端口真的拒连）。

## 5. 未验项

- `G21b-real-app-inside-subframe`：把真实应用装进同来源 iframe 时，应用会把**顶层文档**导航成自己的 URL（frame-bust），会毁掉夹具自己的落点文档 ⇒ 从夹具移除并记 **notRun**；"子框架无能力"由 G19/G21/G22 + D15 策略覆盖。
- `G33-busy-turn-is-real`：点击发送后 30 s 采样未观察到"停止生成/非 plain"⇒ 操作 10 只采用确定可达部分（input 禁用 ⇒ `file-input-disabled`，回合中导入按实际接受/拒绝如实记账）。
- `G38-subagent-session-specific-state`：`session/list` 无可识别子代理会话（造它需真实嵌套回合）⇒ 只覆盖"瞄准非当前会话 ⇒ `context-changed`"。
- `d09-loop-gates.mjs` 本轮未跑（预算）。
- **Desktop 真实焦点 / WebView origin 按指示记为 WindowsPending**，未尝试。
- 环境不稳定：headless Chromium 偶发渲染进程失联（前几次 runId 里可复现）；已用超时 + 重开 + 完整性判定把它变成**确定失败**而不是"没有证据"。

## 6. 顺带修复与观察

- **未发现需要改 `src/**` 的产品缺陷**（src 一行未改）。
- 测试基建：`provider-stub.mjs` 新增可选 `holdOpenMs`（默认 0，既有判据逐字节无变化）；第二 target（3097）+ `service-b.pid` 的收口已在 `setup.sh`/`down.sh` 里预置（子代理未改这两个文件）。
- 观察（**非本插件缺陷**）：真实应用在同来源 iframe 内会 frame-bust 顶层文档——已按要求记为 notRun，未掩盖。

## 7. 状态

D20 **done**：隔离矩阵 48 行**误投递 0**、43/43 判据、无泄漏且 canary 可触发、`plugin-integration-suite` 由 `incomplete` 转为 **pass**（Development 未覆盖项 3 → 2）。D21 依赖解除。
