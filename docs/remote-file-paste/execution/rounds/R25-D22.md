# R25 · D22 · 最终 tarball 的干净安装

方案版本 2.0。候选：工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c…`（未提交）。本轮把门禁里**最后一个** `incomplete`（`final-tarball-install`）实现为真。

本轮任务：D22。状态 **done**。**Development 未覆盖项 1 → 0**（全部 30 项已实现）。

## 1. 交付

| 项 | 内容 |
| --- | --- |
| 门禁 | `tests/fixtures/d22-tarball-gates.mjs`（npm `fixture:tarball`）：**26/26 判据、13/13 负向探针、exit 0** |
| 干净安装脚本 | `tests/fixtures/d22-clean-install.sh`（夹具私有 `DSH_HOME` 内新建 profile `dsh-attachments-d22`） |
| 共享卫生扫描 | `scripts/pack-scan.mjs`（`test-pack.mjs` 改为引用**同一条**扫描）+ 单测 `test/unit/pack-scan.test.mjs`（7 例含 canary） |
| manifest | `artifacts/verify-portable/d22-package-manifest.json`（+ runId 副本、夹具副本） |
| 证据 | 双写 `artifacts/{verify-portable,fixture/evidence}/d22-tarball-gates.json`（两份逐字节相同） |
| 历史 | `artifacts/verify-portable/d22-runs/<runId>/`（report/gates/run/stdout/manifest/composed-config/served-index）+ 只追加的 `index.json`（6 次运行） |
| 门禁接入 | `eng/verify-portable.ps1` 新增 `final-tarball-install` 步骤；`eng/verification-profiles.json` 加 `finalTarballInstall` 段 |

## 2. 关键结论（"跑着的就是本轮 tarball"）

- 候选包 **sha256 `aa2da4a3781ec20835df229cba72e14f762ccb94d5c0d8e190c4a9aa010606b5`**，**integrity `sha512-yLufZVBqcWK1WNoeQJbQ90Cd26ABG+yF43d+sHYpim65ebsIVZ6rFsVRKrJX5r504cYCXLsSgJHX8ibrswmdBw==`**；包内 `lib/**` 与当前构建树 **55 个必发文件逐字节相同**（陈旧 `pack/*.tgz` 会被抓出）。
- **干净安装**：夹具私有 `DSH_HOME` 里新建 profile `dsh-attachments-d22`（**此前不存在**），固定 `@linxin666/dsh-web-all@0.3.20` + 候选 tarball；profile 内 **59 个文件与包内逐字节相同**，lockfile 记录到候选 integrity/tarball，**无 link:/符号链接**。
- **服务的确是本轮包**：服务返回的 HTML 逐字含**已安装 host 模块现场生成**的承载脚本（raw 模式、2980 字符）；合并 client bundle 里本包 `lib/client.js` 的**完整字节恰好出现一次**且落在 combo 接缝上（sha256 `d07b4e1895b4…` == 包内）。

## 3. 代表性闭环（干净 profile、真实 Chromium、LAN `/remote` 配对通道）

11467 B 分 3 块（≤4096 B/块）全部 ack → `file-end` 哈希通过 → 草稿 `staged` → **原生 id `7fc46730-ca33-4e55-ba2b-9a8a959a2c84`** 由下一批 `previous` 逐位回读 → 上传 `POST /remote/api/session/uploadFileBinary?name=d22-clean-profile-note.txt` **200 + 设备头**，receipt `bytes=11467`、`attachmentId=sha256:66c95445…` → 点击发送 `agentRequests 0→2`、工具调用 1 次 → **真实 `read` 读回 11467 B / sha256 `66c95445…`，与导入逐字节相同**；主机附件库另有同名同哈希暂存副本。

## 4. 无额外 remote 实例 + 包卫生

- **无额外 remote**：组合配置 `addonRows=1 / remoteAggregatedRows=1 / remoteDirectRows=0 / webserverRows=1`；运行期**只有 1 个 dsh 进程**，3099/3097 端口未开，remote 进程 0；client roster 无 remote。
- **包卫生**：59 文件 **0 命中**；cookie / credential 路径 / 绝对家目录 canary **全部触发**（证明扫描会响）；manifest 与证据过 `redact()` + `findSecretLeaks`，**0 泄漏且 canary 命中**。

## 5. 顺带修复（真实产品缺陷，最小改动）

`src/client/compose.ts` 的 `writeDiagnostics`：capability 由**安装期快照**改为**读时探测（getter）**。症状：boot 早期写入的快照会**长期谎报** `unavailable`/missing `@deepseek-ai/dsh-client-ui-conversation`，而接收端 `file-end`/草稿导入实际成功（`__DSH_ATTACHMENTS_ADDON__.status()` 实时为 `available`）——即诊断面与真实能力不一致。修后单测 **167/167**、D13 浏览器判据 **16/16 + 9/9** 全绿。

## 6. 复验（本机实跑，我独立核对）

| 项 | 结果 |
| --- | --- |
| D22 证据 | `result=pass`、`failedGateIds=[]`、**26 条判据**（我核对） |
| eng 接入 | `final-tarball-install` 已在 `eng/verify-portable.ps1`（我核对 2 处引用） |
| runId 证据 | `d22-runs/` 多个 runId 目录 + 只追加索引（我核对 ≥4 个目录） |
| 固定用例基线 | 未受影响（Acceptance 57 / Core 546 / Windows 67）（我核对） |
| 回归 | `pnpm run verify`（167/167、test:pack 10/10）、`test:wire` 9/9、`d13-bridge-gates` 16/16 + 9/9 |
| Development profile | 见 state.json（本轮 incomplete 目标为 0） |

**残余核验缺口（如实记录，且这是第三次同类失误）**：我脚本抽读 manifest 的 `tarballSha256`/`integrity` 时**又用错了键名**（打印 None），未亲自核对 manifest 里的 sha256/integrity 字段位置；上述数值采信子代理报告。**我连续三轮（D19/D21/D22）都在"猜字段名"上出错**——已写入交接文档，作为我这一侧的方法缺陷。

## 7. 未验项

- **WindowsPending**：Windows 打包/安装（MSI/zip、真实 WebView2 承载、`pwsh` 跑 `eng/verify.ps1`、Windows 字节源）——已在证据里列为 notRun，未尝试。
- 子代理**未跑** `d09`/`d20`/`d21`（夹具经济性；`compose` 改动由 D13 浏览器层覆盖）。
- 注意：`d13` 结尾调用了 `down.sh`，**夹具根目录已删除**（只剩 `evidence/`）——后续需要夹具时得重跑 `setup.sh`。

## 8. 状态

D22 **done**：候选 tarball 在**全新隔离 profile** 上干净安装并跑通代表性闭环，"跑着的就是本轮包"由**三重证据**闭合（安装侧 integrity == 新打包产物 / profile 内 59 文件与包内逐字节相同 / 服务出的 client bundle 含本轮 `lib/client.js` 完整字节且 sha256 相同），无额外 remote 实例、包卫生 0 命中且 canary 可触发。**Development 未覆盖项归零（0）**，`final-tarball-install` 转为 pass。D23 依赖解除。
