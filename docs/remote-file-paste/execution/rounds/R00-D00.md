# R00 / D00

用户执行指令：读取 deepseek-harness-prompt.md，严格执行实施正文与分轮规则；本轮只执行 D00，核实基线、建立任务台账和交接记录，不修改业务代码，不开始 D01，完成后结束本轮等待“继续”。

方案版本与被测候选摘要：方案 2.0。候选为 Launcher 仓库工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c3f7a0a34d6ed4fdd7804e369bdd0258b`，工作树干净、无未跟踪文件、无 stash、与 `origin/main` 无差异。该提交与研究基线快照一致。本轮无业务代码候选，无构建产物。

本轮范围：仅 D00。只读核实工作树、工具链、上游版本与任务图，并把方案包放入仓库、建立可恢复台账。未开始 D01，未运行构建、测试或任何门禁。

执行前状态：仓库内不存在 `docs/remote-file-paste/`；`execution/state.json` 为初始模板（`round=null`、`baseline=null`、26 项全 `todo`、`developmentStatus=notStarted`）；`execution/handoff.md` 为初始交接模板。判定为从未执行，属“仅在从未执行时初始化 state.json”的情形。

实际变更：

- 新增 `docs/remote-file-paste/`（38 个文件），内容与方案源目录逐字节一致（`diff -r` 无差异）。此前仓库无该目录，不是覆盖已有成果；替换对象是仓库外的旧 `.scratch` 方案。
- 新增 `docs/remote-file-paste/execution/baseline.json`：实际版本、工作树摘要、工具清单与未核实项。
- 新增本文件 `docs/remote-file-paste/execution/rounds/R00-D00.md`。
- 更新 `docs/remote-file-paste/execution/state.json`：D00 → done，登记 round、lastRound、checkpoint、remaining、blocker、evidence 与 baseline 块。
- 更新 `docs/remote-file-paste/execution/handoff.md`：改为 R00-D00 实际交接。
- 未改动任何业务代码、项目文件、锁文件、`eng/` 脚本、`schemas/`、`installer/` 或生产 profile；未安装或发布任何包；未推送远端。

实际验证（命令、平台、退出码、用例数、报告路径）：

| 检查 | 命令要点 | 平台 | 退出码 | 结果 |
| --- | --- | --- | --- | --- |
| git 基线 | `git rev-parse HEAD`；`git status --porcelain --ignored`；`git stash list`；`git rev-list --left-right --count origin/main...HEAD` | Linux | 0 | 干净；HEAD=df68795…；无未跟踪/无 stash；ahead/behind = 0/0 |
| 方案复制一致性 | `diff -r <方案源> docs/remote-file-paste` | Linux | 0 | 38 文件逐字节一致 |
| 任务图校验 | python3：DFS 三色标记查环 + 依赖/父项/卡文件/state 条目集合核对 | Linux | 0 | 26 任务、26 唯一 ID、无重复、无环、无悬空依赖、无非法 parentId、无缺卡；state 与图 ID 集合相同 |
| 方案内链接 | python3：扫描 `docs/remote-file-paste/**/*.md` 相对链接 | Linux | 0 | 检查 65 条，断链 0 |
| 工具链 | `dotnet --version/--list-sdks/--list-runtimes/--info/workload list`；`node -v`；`npm -v`；`pnpm -v`；`pwsh -Command $PSVersionTable`；`git --version`；`python3 --version` | Linux | 0 | 见下 |
| 浏览器 | `ls ~/.cache/ms-playwright`；`google-chrome --version` | Linux | 0 | playwright `chromium-1243`（含 `INSTALLATION_COMPLETE`/`DEPENDENCIES_VALIDATED`）；Google Chrome 151.0.7922.173 |
| 上游版本 | `npm view @linxin666/dsh-web-all`、`@linxin666/dsh-remote-web-ui`、`@playwright/test`（只读） | Linux | 0 | latest 均 0.3.22（playwright 1.63.0）；0.3.21 与 0.3.22 integrity 已记录 |
| Node 能力 | `node -e` 引擎区间判定与 `module.stripTypeScriptTypes` 检测 | Linux | 0 | 满足 `^22.19.0 \|\| >=24.0.0`；`stripTypeScriptTypes` 为 function |

实测工具链（本轮无构建，故无用例数）：

- OS：Ubuntu 26.04 LTS，内核 7.0.0-30-generic，x86_64，RID linux-x64。
- .NET SDK：10.0.400，位于 `$HOME/.dotnet`，与 `global.json`（`rollForward: disable`）一致。运行时 `Microsoft.NETCore.App` 10.0.11、`Microsoft.AspNetCore.App` 10.0.11；**无 `Microsoft.WindowsDesktop.App`**（Linux 上 WPF 的预期限制）。已安装工作负载：无。
- Node v22.23.2、npm 10.9.8、pnpm 11.7.0、PowerShell 7.5.4、git 2.53.0、python3 3.14.4。
- `dotnet` 与 `pwsh` 均不在默认 PATH。可用环境脚本 `/tmp/dsh-env.sh` 设置 `DOTNET_ROOT` 并把 `$HOME/.dotnet`、`$HOME/tools/pwsh` 加入 PATH。该脚本位于 `/tmp`，重启后可能丢失，属 D01/D02 前需处理的复现前提，已登记。
- NuGet 缓存 26 个包，已含锁定版本所需的 `Microsoft.NET.Test.Sdk 18.9.0`、`xunit.v3 4.0.0`、`xunit.runner.visualstudio 4.0.0`、`coverlet.collector 10.0.1`、`Microsoft.Web.WebView2 1.0.4129.50`。npm registry 可达（本轮仅只读查询）。

上游版本核实结果（重要偏离）：研究快照为 dsh-web commit `0d78d391b5f67ec4d9f04d47558b76352c74ee48`、all/remote 0.3.21、Harness `c291e7961a515f6d7af9304e7fd1d257929aef26` 0.1.5-rc.2。本机实际部署为 `@linxin666/dsh-web-all` 0.3.20、`@linxin666/dsh-remote-web-ui` 0.3.20（profile `$HOME/.dsh/profiles/web`，依赖声明 `^0.3.20`），已安装 Harness 为 `@deepseek-ai/dsh` 0.1.5-rc.1。三者都不等于研究快照，也不等于 npm latest。方案明确要求固定 lock、integrity 与测试 profile、禁止浮动 latest；版本选定属 D04，本轮只登记偏离与候选 integrity，不做选择。

目标框架核实：`Directory.Build.props` 当前给全部六个项目统一 `net10.0-windows` + `win-x64`，六个项目均未覆盖。这确认 D01 的必要性与其范围；本轮未改动这些属性。六份 `packages.lock.json` 均在位。

未执行项与所属轨道：

- `dotnet restore --locked-mode`、`format`、`build`、Core 测试执行与用例数核对、全 solution 交叉构建（L12）——D00 范围外，且本轮不产生构建产物；属 DEV/D01–D02。方案记载的基线用例数 Core 49 / Platform 25 / Acceptance 35 本轮**未复核**。
- `eng/verify-portable.ps1`——尚未实现，属 D02。
- `eng/verify.ps1`——Windows 正式门禁，不在 Linux D00 范围；属 Windows 轨道。
- `probes/probe-remote-upload-boot.mjs`——需要 dsh-web checkout 路径参数，本机无该 checkout，无法运行；研究记录的 Windows 7/7 PASS 不计为 Linux 证据。
- 真实 Harness＋全家桶夹具、Chromium/Playwright 浏览器测试——属 D04/D05/D12/D18，本轮不启动任何服务。
- Windows 编译、剪贴板、WPF、WebView2、安装验收——无 Windows 环境，属 WindowsPending。
- 以上未执行项一律记 notRun，不解读为 pass。

本轮结果：done。

D00 验收对照：本轮没有业务代码、生产 profile、安装包或远端发布变化 ✅；台账实际值均来自查询 ✅；未运行检查明确记 notRun、未写入 pass ✅；D00 之外没有任务开始 ✅；方案与仓库均存在，无缺件 ✅。任务图无环、26 个任务全部可追踪 ✅。

已保留成果：`docs/remote-file-paste/` 完整方案包（38 文件，可交付件随包）、`execution/baseline.json` 实测基线、本记录、`state.json` 与 `handoff.md` 台账。工作树此前无用户改动，本轮新增仅为文档与台账。

剩余步骤或解除条件：无。D00 无阻断。唯一的开放决策（上游版本固定）已指定由 D04 处理，不构成本轮阻断。

本轮仍运行的后台写入：无。本轮未启动任何后台作业、子代理、构建或服务。

下一轮建议：仅建议，不自动执行。按依赖图，D00 完成后 D01（分离 Core 的平台目标）与 D03（建立独立附加插件骨架）两项依赖均已满足，其中 D01 编号最小。建议下一轮执行 D01：把 Core/Core.Tests 改为 `net10.0`、去掉 `win-x64` RID，同时保持另外四个项目的 Windows 定位与原有正式门禁强度。执行前需先确认 `dotnet`/`pwsh` 的 PATH 准备方式仍可用，并在该轮首次运行 `dotnet restore --locked-mode`。Windows 实机验收、安装、发布与推送均需用户另行明确授权。
