# R26 · D23 · 兼容、停用与更新后状态

方案版本 2.0。候选：工作树，分支 `feat/remote-file-paste-attachments`，HEAD `df68795c…`（未提交）。

本轮任务：D23。状态 **done**。Development 门禁新增第 31 项检查 `addon-compatibility-fixture`，**31/31 checks pass、incomplete 0、`portableStatus=pass`**。

## 1. 交付

| 项 | 内容 |
| --- | --- |
| 门禁 | `tests/fixtures/d23-compat-gates.mjs`（npm `fixture:compat`）：**18/18 判据 + 11/11 负向探针、exit 0**（约 282 s） |
| 兼容矩阵 | `artifacts/verify-portable/d23-compatibility-matrix.json`（**5 行**） |
| 版本策略 | `plugins/dsh-remote-attachments/docs/COMPATIBILITY.md`（插件内 docs，未动仓库级 `docs/**`） |
| 证据 | 双写 `artifacts/{verify-portable,fixture/evidence}/d23-compat-gates.json`；`d23-runs/<runId>/`（12 个目录）+ 只追加索引 |
| 门禁接入 | `eng/verify-portable.ps1` 新增 `addon-compatibility-fixture`；`verification-profiles.json` requiredChecks **30 → 31** + `compatibilityAndDisable` 段 |

## 2. 兼容矩阵（行 × 能力/原因码 × 配对心跳 × 内置回退）

| 组合 | 能力 | 原因码 | 配对/心跳 | 内置回退行为 |
| --- | --- | --- | --- | --- |
| verified | available | — | 不受影响 | 走承载 → `/remote` 上传 200，主机库逐字节一致 |
| unknown-hook | unavailable | `capability-conflict` | 不受影响 | **未知 hook 原样保留**并继续承担内置上传 |
| remote-degraded | unavailable | `capability-disabled` | 不受影响 | 承载拒绝、**零请求**（不回退裸 `/api`） |
| unsupported-version | unavailable | `capability-disabled` | 不受影响 | 内置路径与原厂基线页同形 |
| addon-disabled | LAN unavailable / loopback degraded | `capability-disabled` | 不受影响 | 与原厂基线（整行 disabled）逐项一致；loopback 内置上传落库逐字节一致 |

（我独立核对：矩阵 5 行、**每行 `pairingUnaffected=true`**、原因码齐全。）

## 3. 独立停用 / 恢复证据（验收核心）

- **运行时停用**：自有三全局 + 承载被撤、状态 `disposed` + `reload-required`、**保留引用也被拒**；**心跳 200、`/remote` 读会话 200、设备集合哈希不变**；**`PairingService.stop` 插桩记录恒为 0**。负向对照 N10：**真调一次 `stop()`** 时插桩 0→1、设备失效、设备库清空——证明"没丢设备"不是空判据。
- **页面重载**：真实 `Page.reload`（loopback，Harness 自己服务文档）后恢复谓词 **false→true**，且**真实批次再次进草稿 + 主机库逐字节一致**。**LAN 页面的裸 reload 被上游浏览器凭据层 401**（干净页面对照同样 401，N11）⇒ 受支持的再入路径是 `/pair-app`。
- **重启 Harness**：同一设备凭据仍可用、页面再入后附件通路照常。
- **不支持组合**用夹具内确定性模拟（已装 remote 包的 seat 全局名一致改名，**结束后还原并记录 sha256**）：能力 **fail closed**（`capability-disabled`、承载拒绝零请求），内置路径与原厂基线页同形。

## 4. `ReloadRequired` 判定（双向，正是任务卡验收点）

- **只恢复承载、再恢复全部四个全局** ⇒ `recovered=false`、`reloadRequired.required=true`、导入**仍被 `capability-disabled` 拒** —— 即**"恢复 global 变量 ≠ runtime 恢复"**。
- **真实重载后**同一谓词翻 `true` 且跑通真实批次。

## 5. 版本策略

固定 Harness `0.1.5-rc.1`、all/remote `0.3.20`、插件 `0.1.0`，**实测逐项一致**；`all` 用户主动更新只改写 profile 的 web-all 解析，**本插件行/tarball 不受影响**，核对前一律按不支持组合 **fail closed**；独立 scope 边界 = `dsh plugin --profile <p>` 转发 pnpm，仅影响被点名依赖。**未对任何来源执行更新**（未对真实 npm 源或生产）。

## 6. 顺带修掉两处真实产品缺陷（最小改动）

1. **`src/host/upload-hook.ts` + `src/host/index.ts`**：cordis 浏览器半区加载器 `create({name})` **不带行配置**，client 半区收不到 `enabled:false` ⇒ **禁用插件在 loopback 页面上把自己报成 available 且照旧导入**（禁用失效）。现在 host 在禁用时仍注入一条**只写 `__DSH_ATTACHMENTS_STATUS__.host.enabled=false` 的标记脚本**（不装承载），client 据此判 degraded/unavailable。
2. **`src/client/compose.ts`**：撤销后 `capability()`/`status().capability` 仍按页面事实回报 `available` —— 这正是**"只恢复全局变量 = 已恢复"的假象**来源。现在撤销后的结论**固定为 `unavailable` + `capability-disabled`**（原因带 `reload-required`），facts 仍如实回带。

单测 **167 → 169** 全绿（新增 2 例，1 例按新契约更新）。

## 7. 复验（本机实跑，我独立核对）

| 项 | 结果 |
| --- | --- |
| D23 证据 | `result=pass`、`failedGateIds=[]`、**18 条判据**（我核对） |
| 兼容矩阵 | **5 行**、逐行 `pairingUnaffected=true`、原因码齐全（我核对） |
| Development profile | **31/31 checks pass、incomplete 0、fail 0、`portableStatus=pass`、crossBuildStatus=pass、cases=546/546**（我核对汇总） |
| 新增检查 | `addon-compatibility-fixture` = **pass**（我核对） |
| 插件回归 | `pnpm run verify`（169 单测、test:pack 10/10）、`test:wire`（86 样本）通过；d13 16/16+9/9、d19、d20、d21、d22 在全量运行中重跑通过 |
| 修复在位 | `upload-hook.ts`/`compose.ts` 改动可见、`docs/COMPATIBILITY.md` 在库（我核对；`index.ts` 的匹配用我的关键词为 0，属我关键词不准，非缺失） |
| 清理 | `down.sh` exit 0、残留进程 0、生产 profile 清单哈希漂移 0 |

## 8. 未验项（已在证据 notRun 登记，不计通过）

- **WindowsPackagingAndUpdate**（WindowsPending：MSI/zip、真实 WebView2、Launcher 更新流程）。
- **RealAllBundleUpdate**（未对真实 npm 源或生产执行更新，只从 lock/manifest 推理）；**UnverifiedUpstreamCombination**（`0.3.21`/`0.3.22` + `0.1.5-rc.2` 未安装）；**MarketplaceUpdateFlow**；**RealIndependentScopePluginUpdate**（未装第二个插件版本，边界由组合差异 + 清单/锁文件未变证明）。
- 方法说明：设备表默认 `maxDevices=4` 且每次配对挤掉最老设备，故门禁开头清空**夹具私有**设备库、全程只保留 1 个主设备 + 1 个负向控制设备；两处夹具私有改动（stop 插桩 / seat 改名模拟）都在 `finally` 还原并记录 sha256。
- 我仍未亲自核对矩阵行的"能力状态"字段（该行用了我猜的键名，打印 `None`）——**连续第四轮同类失误**，已再次印证交接里"读 JSON 前先打印键名"的教训。

## 9. 状态

D23 **done**：兼容矩阵 5 行、独立停用/重载/重启三条路径均有实测，**停用不丢配对**（`PairingService.stop` 插桩恒 0 + 设备集合哈希不变，另有能抓到丢设备的负向对照）、**不得谎报恢复**（只恢复 global ⇒ 仍 `reload-required` 且导入被拒；真实重载后才真恢复）。Development 门禁 **31/31 全 pass、incomplete 0、`portableStatus=pass`**（`developmentReady=false` 仍由 profile 配置决定）。D24 依赖解除。
