<Window x:Class="DshLauncher.Desktop.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock x:Name="LabelText" TextWrapping="Wrap" Margin="0,0,0,8" />
        <TextBox x:Name="ValueText" Margin="0,0,0,12"
                 TextWrapping="Wrap" AcceptsReturn="False"
                 SpellCheck.IsEnabled="False" />
        <TextBlock x:Name="ExtraLabelText" Text="显示名称（可选）" Visibility="Collapsed" Margin="0,0,0,8" />
        <TextBox x:Name="ExtraText" Visibility="Collapsed" Margin="0,0,0,12" MaxLength="64" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="HintText" Foreground="#888" TextWrapping="Wrap"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="取消" Padding="14,6" Margin="0,0,8,0" IsCancel="True" />
            <Button Grid.Column="2" Content="确定" Padding="14,6" Click="OnConfirm" IsDefault="True" />
        </Grid>
    </StackPanel>
</Window>
