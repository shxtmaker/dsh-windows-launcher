# DeepSeek Harness Windows 启动器

本上下文描述 Windows 入口程序与既有 Linux Harness 服务之间的产品边界和连接语言。

## Language

**Windows 启动器**：
在独立窗口中呈现一个或多个既有 Harness 实例 Web UI 的 Windows 客户端。它不拥有远端服务进程的生命周期。
_Avoid_: Harness 管理器、远程桌面客户端

**正式支持环境**：
启动器发布验收和兼容性承诺覆盖的 Windows 客户端环境。首版仅指 Windows 11 25H2 或更高版本的 x64 系统。
_Avoid_: 可运行环境、最低启动版本

**兼容运行环境**：
不在正式支持范围内，但启动器不主动阻止且尽力保留基本可运行性的 Windows 客户端环境。Windows 10 x64 属于此类，其专属问题不构成常规发布阻断。
_Avoid_: 正式支持环境、受支持 Windows 10

**资源参考配置**：
用于测量目标窗口并发资源曲线的指定 Windows 11 硬件配置。它是可重复性能证据的基准，不是最低硬件要求。
_Avoid_: 最低配置、支持下限、推荐配置

**依赖基线**：
V1 经源码契约核实并锁定的 Harness、LAN 插件、WebView2 SDK 与发布时 Runtime 版本组合。它只能通过显式刷新和重新验收改变。
_Avoid_: 当前版本、最新版本、浮动依赖

**验证门禁**：
候选版本进入发布确认前必须完成的统一自动验证和最终安装包实机冒烟。记录性兼容或压力观察不属于门禁。
_Avoid_: 验收证据层级、P0/P1/P2、构建成功

**发布阻断项**：
正式候选版本在验证门禁中必须通过且不得以记录性结果替代的产品、安全、数据或发布物要求。失败必须修复或完成明确根因归类后重新验证。
_Avoid_: 严重问题、已知问题、记录性检查

**同时活动窗口承诺**：
V1 在资源参考配置上保证可同时保持工作的独立目标窗口数量，固定为四个。它与目标目录中可保存的目标数量无关。
_Avoid_: 目标数量上限、压力测试峰值、窗口总数

**参考测试拓扑**：
为重复验证跨 Windows、局域网和多个 Linux Harness 主机行为而固定的环境与服务角色集合。它包含正常服务、错误服务和可控故障夹具。
_Avoid_: 测试环境、开发环境、单机冒烟

**实施交接包**：
共同构成 V1 实施依据的实施方案与发布检查表。前者包含开发边界、里程碑和 Linux 前置，后者包含验证、证据和人工发布确认。
_Avoid_: 最终文档、任务清单、README

**发布常量清单**：
为一个正式发布身份锁定产品名称、应用标识、工具与 Runtime 版本、发布地址及签名主体的构建输入。存在占位值的清单不能生成正式产物。
_Avoid_: 构建配置、环境变量、版本备注

**正式候选版本**：
发布常量完整、产物身份固定并准备接受验证门禁的版本。它在精确安装包通过门禁并获得明确人工发布确认前不是正式发布。
_Avoid_: 内部包、测试构建、正式发布

**发布验证报告**：
绑定候选版本提交、安装包哈希、依赖基线、执行环境、门禁结果和人工发布确认的最小持久记录。失败原始日志是临时诊断材料，不属于该报告。
_Avoid_: 证据仓库、测试录像、原始日志归档

**原生诊断导出**：
用户从目标中心主动生成的脱敏 Windows 诊断包。它不属于 Harness 或 WebView 下载，也不包含浏览器会话、页面内容或用户附件。
_Avoid_: Harness 下载、日志目录、crash dump

**正式安装包**：
V1 唯一获得发布承诺的按用户 EXE 安装产物。便携 ZIP、MSIX 和解压运行副本不属于正式交付。
_Avoid_: 便携版、MSIX 包、免安装版

**本地固定卷**：
Windows 将安装路径最终解析到的本机固定存储卷。网络映射、UNC、可移动设备和无法确认最终位置的重解析路径不属于本地固定卷。
_Avoid_: 本地硬盘、任意盘符、安装盘

**应用安装目录**：
正式安装包在本地固定卷上为当前 Windows 用户部署已签名程序与自包含依赖的位置。首次安装后其身份随安装登记保持；目标配置、浏览器会话和其他用户状态不属于该目录。
_Avoid_: 应用数据根、配置目录、WebView 数据目录

**应用数据根**：
当前 Windows 用户持有启动器配置、逐目标浏览器会话和诊断状态的稳定本地位置。它独立于应用安装目录，并在程序文件替换时保持身份不变。
_Avoid_: 应用安装目录、临时目录、下载目录

**运行时下限**：
某个启动器版本已经验收并要求的最低 Evergreen WebView2 Runtime 版本。兼容的更高版本可用，不要求与构建时版本精确相等。
_Avoid_: 最新 Runtime、Fixed Version、构建时版本

**就地升级**：
用户运行较新正式安装包，在保持应用身份与应用数据根不变的情况下替换当前程序文件的升级方式。它不是后台自动更新或覆盖解压。
_Avoid_: 自动更新、重新安装、文件覆盖

**修复安装**：
用户重新运行相同版本正式安装包，以恢复程序文件或缺失运行时前置条件且不改变应用数据根的操作。它不迁移数据，也不代表降级。
_Avoid_: 就地升级、重新配对、数据恢复

**应用数据迁移**：
新版启动器首次打开既有应用数据根时，对已知旧模式执行的带备份原子转换。安装程序不拥有该过程，未知新模式不能作为迁移来源。
_Avoid_: 安装脚本迁移、就地升级、降级

**数据清除卸载**：
用户在卸载时额外明确选择删除当前 Windows 用户全部启动器配置与配对会话的不可逆模式。普通卸载默认保留应用数据根，不属于数据清除卸载。
_Avoid_: 普通卸载、忘记目标、清理缓存

**Harness 主机**：
运行一个或多个 DeepSeek Harness 实例与 LAN 访问插件，并拥有这些服务进程和监听端口的 Linux 系统。
_Avoid_: 启动器服务器、远程桌面

**Harness 实例**：
Harness 主机上通过一个独立目标端点提供 Web UI 的 Harness 服务。同一主机的不同端口表示可共存的不同实例。
_Avoid_: Harness 主机、目标列表

**目标端点**：
由协议、主机或 IP 以及端口共同标识的 Harness Web origin。相同 IP 上的不同端口是不同目标。
_Avoid_: IP 地址、服务器地址

**候选目标**：
用户正在验证、尚未进入目标目录的 IP 与端口组合。它不是可供自动连接的已配置目标。
_Avoid_: 临时配置、待登录服务器

**待配对目标**：
已经进入目标目录、但尚无已提交浏览器会话的目标配置。它不是候选目标，也不代表目标当前离线。
_Avoid_: 候选目标、失败目标、离线目标

**已配对目标**：
拥有已提交且与自身目标端点完全绑定的浏览器会话的目标配置。它仍可能离线、被网络策略阻止或暂时不可用。
_Avoid_: 在线目标、可用目标、默认目标

**目标配置**：
Windows 启动器为一个目标端点保留的持久选择、显示名称与网络信任确认。它不包含配对链接、浏览器会话或最近连接结果。
_Avoid_: 登录配置、浏览器配置、服务器配置

**目标标识**：
一个目标配置正式进入目标目录时获得的不可变本地身份。端点变化或忘记后重新添加会形成新的目标标识，重命名和改变默认目标不会。
_Avoid_: 目标端点、IP 地址、目录序号

**显示名称**：
用户为目标配置设置的可选、可重复本地标签。它不参与目标身份或安全判断。
_Avoid_: 主机名、唯一名称、登录名

**目标目录**：
当前 Windows 用户保存的目标配置集合，以规范化目标端点去重。它可以同时包含不同主机及同一主机不同端口的目标。
_Avoid_: 当前目标、服务器列表、连接历史

**默认目标**：
目标目录中被用户选定为启动器启动后自动打开的唯一目标配置。目标目录非空时恰有一个默认目标；它只是本地启动入口，不代表 Harness 主机之间存在主从关系。
_Avoid_: 主服务器、主 Harness、活跃目标

**忘记目标**：
从当前 Windows 用户的目标目录中移除一个目标配置、网络信任确认和浏览器会话的本地操作。它不停止或删除远端 Harness，也不影响其他目标。
_Avoid_: 删除服务器、卸载 Harness、远程注销

**目标窗口**：
独立呈现一个目标端点 Harness Web UI 的 Windows 内容窗口。同一目标最多打开一个目标窗口，不同目标可以同时打开。
_Avoid_: 浏览器标签页、远程桌面窗口

**目标内容宿主**：
在一个目标窗口内承载 Harness Web UI，并始终绑定同一目标标识、目标端点和浏览器会话的本地能力边界。它限制浏览器行为并恢复页面容器，但不拥有 Harness 业务或远端进程。
_Avoid_: 通用浏览器、原生桥接、Harness 客户端实现

**契约兼容 WebUI**：
由 Harness 目标端点呈现、沿用其浏览器会话，并满足启动器兼容契约的第三方界面。它可以使用契约明确容纳的外部依赖，但不是任意独立网站。
_Avoid_: 所有第三方 WebUI、任意 WebUI、通用网页

**基础兼容**：
契约兼容 WebUI 只依赖目标端点同源资源和全局允许的浏览器能力时获得的兼容层级。它不要求专属适配规则。
_Avoid_: 默认放行、无条件兼容、尽力支持

**扩展兼容**：
契约兼容 WebUI 的外部资源或受限嵌入依赖经过明确审核并纳入适配规则时获得的兼容层级。它不能突破目标内容宿主的全局安全上限。
_Avoid_: 特权模式、完全信任、通用浏览器模式

**WebUI 兼容描述符**：
由目标端点提供、用于声明 WebUI 稳定身份、精确版本和适配需求的不受信任同源信息。它表达需求但不认证页面，也不授予浏览器能力。
_Avoid_: 权限清单、自动授权、HTML 指纹

**WebUI 适配规则**：
由 Windows 启动器信任并用于判断某个 WebUI 身份、版本和依赖是否满足扩展兼容的兼容记录。它只在全局安全上限内生效。
_Avoid_: 页面自授权、任意白名单、安全例外

**参考适配器**：
用于证明通用兼容契约和适配规则注册表能够覆盖真实 WebUI 的标准样例。它与其他适配规则使用同一授权路径，不拥有专用白名单或额外能力。
_Avoid_: 特殊适配器、受信 WebUI、内置放行模式

**提供方一致性套件**：
第三方 WebUI 提供方用于证明某一精确组件版本满足兼容契约的可复现测试集合。通过它只形成审核材料，不产生适配规则或扩展授权。
_Avoid_: 自动认证、兼容授权、Windows 白名单生成器

**兼容接入包**：
第三方 WebUI 提供方针对一个精确组件组合提交的可复现描述符、依赖、责任归属和一致性证据集合。它是 Windows 审核输入，不产生兼容授权或支持状态。
_Avoid_: 适配规则、授权申请、提供方白名单

**兼容审核决定**：
Windows 维护方针对一个不可变兼容接入包作出的可审计技术结论。它可以接受、拒绝或要求补充，但只有随正式签名版本发布后才能产生扩展兼容支持。
_Avoid_: PR 批准、发布确认、兼容支持记录

**契约支持声明**：
Windows 启动器对全部契约兼容 WebUI 使用同一兼容分类和强制模型的正式承诺。它不表示生态中每个第三方 UI 都经过逐项测试或背书。
_Avoid_: 全生态认证、全部页面测试通过、静态支持名单

**基础契约符合**：
某一精确 WebUI 版本通过提供方一致性套件并只依赖基础兼容能力的证据状态。它不表示该版本已经由 Windows 逐项验收或获得性能承诺。
_Avoid_: Windows 已认证、完全支持、自动兼容

**扩展兼容受支持**：
某一精确组件版本组合已命中内置适配规则并通过适用 Windows 门禁的正式支持状态。组件、规则或能力变化后不得沿用该状态。
_Avoid_: 扩展规则候选、提供方自认证、相邻版本兼容

**兼容证据身份**：
把兼容测试结果唯一绑定到精确 WebUI、启动器、受信规则和运行基线的不可分割身份。任一组成发生变化时，旧证据不能直接继承。
_Avoid_: 测试结果、当前版本、相近配置

**兼容支持记录**：
说明一个精确兼容证据身份当前获得何种 Windows 兼容承诺的版本化信息记录。它只用于发布和维护沟通，不参与运行时授权。
_Avoid_: 适配规则注册表、在线白名单、运行时授权目录

**兼容支持暂停**：
因证据、安全或上游状态尚未解决而暂时停止为一个精确兼容证据身份作出新的支持承诺。它不改写历史证据，也不远程改变已安装启动器的适配规则。
_Avoid_: 规则撤销、远程停用、历史证据失效

**兼容治理复核**：
按固定周期确认兼容支持记录中的维护联系人、来源、许可证和外部依赖仍然成立的管理检查。它不代替技术重验，也不使已安装规则按时间自动失效。
_Avoid_: 重新认证、规则保鲜、在线授权检查

**受影响能力群组**：
因同一 Runtime、Harness、契约或全局能力变化而需要共同分析和重验的一组精确兼容证据身份。它只确定验证扇出，不形成共享授权。
_Avoid_: 适配器白名单、全局放行、统一规则

**适配规则注册表**：
Windows 启动器允许参与扩展兼容计算的受信 WebUI 适配规则集合。WebUI 描述符、页面脚本和用户确认均不能向其中添加或修改规则。
_Avoid_: 插件市场、用户白名单、远程配置

**内置适配规则注册表**：
随一个正式签名启动器版本交付、在该安装版本内不可变且离线有效的适配规则注册表。更新它必须安装新的正式启动器版本。
_Avoid_: 在线规则库、独立规则包、本地规则文件

**注册表版本**：
一份内置适配规则注册表规范化内容的单调递增身份。任一规则内容、版本范围或撤销状态变化都会形成新的注册表版本。
_Avoid_: 启动器版本、规则版本、更新时间

**规则撤销**：
较新正式启动器版本停止一条既有适配规则产生扩展能力并使相关外部能力确认失效的安全状态。用户不能覆盖规则撤销。
_Avoid_: 删除配置、临时禁用、远程封禁

**规则撤销墓碑**：
内置适配规则注册表中永久保留既有规则身份但不再产生任何允许能力的撤销记录。它用于阻止规则身份复用并提供脱敏诊断。
_Avoid_: 删除规则、禁用开关、远程黑名单

**注册表回退水位**：
应用数据根中记录的、已经由当前 Windows 用户成功使用过的最高注册表版本。较低内置版本不能越过该水位恢复扩展兼容。
_Avoid_: 当前版本、更新记录、最低支持版本

**全局能力上限**：
目标内容宿主对任何目标页面和 WebUI 适配规则一律生效、不可突破的浏览器能力边界。依赖超出该边界的界面不属于契约兼容 WebUI。
_Avoid_: 插件权限、适配器例外、完全信任

**页面能力集合**：
一个目标页面中全部已匹配 WebUI 适配规则在全局能力上限内合并形成的有效能力。由于同源代码共享浏览器主体，它不能把单次请求归属或隔离到某个插件。
_Avoid_: 插件独占权限、逐插件沙箱、调用者授权

**页面能力快照**：
目标内容宿主在可见页面加载前，根据受信适配规则和全局能力上限固定的一次页面能力集合。页面脚本不能在该页面生命周期中扩大或修改它。
_Avoid_: 动态权限、运行时授权、页面权限状态

**外部能力确认**：
用户针对一个目标和一份精确页面能力快照接受或拒绝已审核外部依赖的本地决定。它不能创建规则、修改能力或突破全局能力上限。
_Avoid_: 用户授权、添加白名单、信任网站

**条件兼容能力**：
只有在指定 WebView2 Runtime 形成实机证据并被当前兼容契约明确纳入后才能承诺的浏览器能力。门禁完成前，它仍属于未支持能力。
_Avoid_: 尽力兼容、实验性放行、默认可用

**功能兼容**：
契约兼容 WebUI 在目标窗口中能够使用其兼容层级允许的能力完成声明功能。它不自动获得资源参考配置下的正式性能承诺。
_Avoid_: 完全支持、性能兼容、可以显示

**性能承诺**：
某一精确 WebUI 或适配器版本在规定资源参考配置和活动窗口矩阵下通过性能门禁的正式承诺。功能兼容不自动产生该承诺。
_Avoid_: 功能兼容、能打开、通用性能保证

**记录性浏览器差异**：
同一目标端点在受支持 Edge 与 Windows WebView2 间不改变兼容授权的观察记录。只有揭示已承诺功能、安全或数据缺陷时才升级为发布失败。
_Avoid_: 兼容失败、自动例外、规则输入

**服务端界面一致性**：
Windows 与 Linux 客户端呈现同一 Harness 目标端点提供、由相同服务端状态选择的界面与功能。它不要求复制浏览器本地状态，也不承诺逐像素相同。
_Avoid_: 像素级一致、浏览器状态同步、界面镜像

**配对链接**：
Harness 主机启动输出中 authority 与待配对目标端点一致、并包含 launch token 的完整 LAN URL，用于建立该目标的浏览器会话。
_Avoid_: 登录地址、目标地址、loopback 启动链接

**受信任局域网**：
使用 RFC1918 IPv4、Windows 专用网络，并由 Harness 主机防火墙限制到指定 Windows 客户端或明确网段的私有网络。仅使用私有 IP 不足以构成信任。
_Avoid_: 内网、任意 Wi-Fi、RFC1918 地址

**浏览器会话**：
Windows 启动器专属于一个目标端点的浏览器状态环境，其核心认证状态是 authority-bound Harness Cookie。Cookie、Web Storage、缓存和导航历史均不与其他目标共享。
_Avoid_: launch token、登录密码

**认证型 Harness**：
要求用 launch token 建立目标端点绑定浏览器会话的 Harness Web 部署。它是首版唯一支持的服务安全代际。
_Avoid_: 最新版、安全服务器

**首次配对**：
目标探测成功后，用配对链接为当前 Windows 用户和目标端点建立浏览器会话的一次性过程。
_Avoid_: 首次连接、登录、自动连接

**配对事务**：
一次使用配对链接为单个目标建立浏览器会话的临时过程。每个目标同时最多有一个配对事务，不同目标的事务相互隔离；只有完整验证后才成为已提交的浏览器会话。
_Avoid_: 登录会话、全局配对、Token 缓存

**自动连接**：
启动器复用未变化目标端点的既有浏览器会话直接打开 Harness Web UI。它不包含首次配对或远端进程启动。
_Avoid_: 自动配对、自动启动

**目标探测**：
首次配对或自动连接前，对目标端点的可达性、Harness 身份和配对需求进行分类的只读检查。它不建立浏览器会话。
_Avoid_: 健康检查、登录、配对

**Harness 能力指纹**：
依赖基线为目标探测固定的一组无副作用 HTTP 状态与受限响应特征，用于识别支持的认证型 Harness 候选。它不是健康端点、版本协商或密码学服务器身份。
_Avoid_: 健康检查、服务认证、版本 API

**图片附件**：
用户通过 Harness 原生会话输入提交给当前 Session 的受支持栅格图片。其内容由 Harness 附件存储所有并成为模型输入，不是 Session 工作目录中的原始文件。
_Avoid_: 图片文件导入、工作区图片、通用文件传输

**通用文件传输**：
后续升级可能提供的、把用户显式选择的 Windows 普通文件复制到当前 Linux Harness 目标的能力。它不属于 V1，也不包含 Linux 到 Windows 下载、双向同步或共享 Windows 目录。
_Avoid_: 图片附件、V1 文件导入、共享文件夹

**Session 工作目录**：
一个 Harness Session 创建时确定、供该 Session 的 Agent 和文件能力解析相对路径的 Linux 目录。V1 图片附件不代表该目录中的普通文件。
_Avoid_: Windows 目标目录、任意上传路径、Workspace 列表项
